

console.log("AAAA")

let mapData;
let playerUnits; // Blue
let enemyUnits; // Red
let playerObjective;
let enemyObjective;

export function printStuff(playerUnits, enemyUnits, mapData) {
    console.log(playerUnits);
}

export function getData(state) {
    console.log(state)
}
